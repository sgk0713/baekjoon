#include <cstdio>
#include <cstring>


int arr[2000001], cache[2000001], T, n;

int dp(int[] num){

	if(!=-1) return num
}

int main(){
	memset(cache, -1, sizeof(cache));
	memset(arr, -1, sizeof(arr));
	scanf("%d", &T);
	for(int i = 0; i<T;i++){
		scanf("%d", &n);
		for(int j = 0; j<n*2; j++){
			scanf(" %d", &arr[j]);
		}
		
	}


	return 0;
}
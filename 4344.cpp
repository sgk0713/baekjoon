#include <cstdio>

int main(){
	int T;
	for(scanf("%d", &T); T--;){
		int n, ave = 0, sum = 0, result = 0, cnt = 0;
		int arr[1001] = {};
		for(scanf("%d", &n); n--;){
			scanf("%d", &arr[i]);	
			sum += arr[i];
		}
		ave = sum/n;
		for(int i = 0; i < n; i++){
			if(arr[i] > ave) cnt++;
		}
		result = cnt*
	}
}	